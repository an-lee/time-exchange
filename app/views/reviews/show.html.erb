<div class="col-md-12">
  <div class="group">
    <%= link_to('Edit', edit_product_review_path(review.product, review), class: "btn btn-primary pull-right") %>
  </div>
  <h2><%= @review.title %></h2>
  <p><%= @reviews.body %></p>
</div>
